{
  "name": "@aurora/aurora-engine",
  "version": "0.1.0",
  "description": "Aurora engine — pipeline orchestrator with HTTP API",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts",
    "test": "tsc && node --test dist/validate-profile.test.js",
    "test:pipeline": "ts-node src/test.ts"
  },
  "dependencies": {
    "@aurora/aurora-memory": "workspace:*",
    "@aurora/aurora-skills": "workspace:*",
    "@aurora/input-processor": "workspace:*",
    "@aurora/safety-layer": "workspace:*",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  }
}
