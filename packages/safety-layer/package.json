{
  "name": "@aurora/safety-layer",
  "version": "1.0.0",
  "description": "Safety validation layer — detects prompt injection, dangerous commands, and emergency situations",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@aurora/input-processor": "workspace:*",
    "@anthropic-ai/sdk": "^0.24.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
