{
  "name": "@aurora/channel-router",
  "version": "1.0.0",
  "description": "Routes AuroraMessages to the agent orchestrator — adapted from OpenClaw for Aurora's local API",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.0",
    "@aurora/input-processor": "workspace:*",
    "@aurora/memory-plugin": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
